# Native receiving-machine check — leave failures visible

The exact patched core was previously tested against actual THRESHOLD through a disclosed managed-browser HTTP/SSE bridge. The new connect helper has separate execution tests. **Neither is native WorkBuddy account evidence.** Fill in a fresh receiving-machine record.

1. Record OS, Python/Git/browser versions and actual WorkBuddy or CodeBuddy version/account execution mode.
2. Import this Skill via the client's real local-Skill installation mechanism; confirm the scripts are accessible to its terminal tool. Do not invent an install path or a successful import.
3. Supply the user-approved public viewer URL + immutable tag/full expected commit, or local trusted release directory. Call `threadline_connect.py connect` from inside the actual client.
4. Verify its JSON says watching=true and returns a real source revision. Open the private launch file locally. Confirm the actual Cytoscape canvas and real project name. A CLI success does not prove browser visibility.
5. Invoke the daily wrapper's `doctor`, `projects`, and bounded `context`. Confirm it sees the same store/project/revision as the viewer.
6. Read real source, map a genuine feature only with authorization, and inspect the evidence. Note that mapping writes `.threadline/features.json`.
7. Have the actual host agent make an approved meaningful source change on a test branch. Do not call `scan` or structural `publish` to make an automatic-path failure appear successful. Observe the watcher advancing the revision and the visible graph updating.
8. Check added/changed/deleted source and temporary historical ghosts; exact node positions/viewport; stale feature interpretation; checkpoint; earlier revision; return to live.
9. Click **Save portable map**, save the actual output, stop the server, and open it independently/offline. Graph/history should remain; source bodies are deliberately absent.
10. Stop/restart and connect a second project with the same viewer installation. Do not regenerate the frontend or silently upgrade the pinned source.

Classify problems as application bug, unsupported repository pattern, local/client environment limitation, or expected behavior. Preserve the failure/error and do not silently switch to prerecorded data. `status` is read-only. `stop` only controls a helper-managed process and preserves history.

No native WorkBuddy marketplace connector, MCP server, cloud website, or new model subscription is created by this Skill. It uses the actual host's authorized local terminal and filesystem capabilities.
